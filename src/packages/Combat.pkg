$#include "Combat.h"
$#include "Engine.h"

class Combat : public Pixy::GameState
{

	static Combat& getSingleton( void );
	static Combat* getSingletonPtr( void );

	//void registerPuppet(Pixy::Puppet* inPuppet);
	//void updateMe(Pixy::Engine* inEngine);
  std::list<Pixy::CPuppet*> const& getPuppets();
  CPuppet* getEnemy(int inUID);
  CPuppet* getPuppet(int inUID);

  void __setIsDebugging @ setIsDebugging(bool setting);
  bool __isDebugging @ isDebugging() const;
};
