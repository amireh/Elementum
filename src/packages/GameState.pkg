$#include "GameState.h"

namespace Pixy {

class GameState {

  enum State {
    Intro,
    Combat
  };

	void changeState( Pixy::GameState *state );
	void requestShutdown( void );
  bool isCurrentState();
};

}
